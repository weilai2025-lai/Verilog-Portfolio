module SR_Latchbev(s, r, q, q_bar);

input s, r;
output reg q;
output q_bar;

assign q_bar = !q;

always @(*)
	begin 
	case({s,r})
		2'b00 q <= q;
		2'b01 q <= 1'b0;
		2'b10 q <= 1'b1;
		2'b11 q <= 1'bx;
		
		default q <= 1'bx;
	end

endmodule